class Solution {
public:
    int maxProductDifference(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        int i =0 ;
        int n = nums.size()-1;
            
            return (((nums[n] * nums[n-1])) - (nums[i+1] *nums[i]));
    }
};
